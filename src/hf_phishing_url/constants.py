DATASET_ID = "pirocheto/phishing-url"

# Dataset label column: `status` with classes: `phishing`, `legitimate`.
LABEL_COL = "status"
URL_COL = "url"

LABEL_MAP = {
    "phishing": 1,
    "legitimate": 0,
}

